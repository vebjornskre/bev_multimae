# Parameters for model spesifications

# dim_tokens: 512    # embedding/token dimention
dim_tokens: 384
num_vfe_features: [64]
depth: 12
num_heads: 8

model_folder: 'model_checkpoints/'
fine_model_folder: 'fine_model_checkpoints/'